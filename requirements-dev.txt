# Dev/test-only dependencies - not installed on Render (Render's build
# command uses requirements.txt directly, per README.md). Install locally
# with: pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.3
