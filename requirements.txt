fastapi==0.115.0
# uvicorn's [standard] extra pulls in websockets>=10.4, which hard-conflicts
# with neo_api_client's pinned websockets==8.1 (see below). This app doesn't
# use uvicorn's websocket/uvloop extras (HTTP-only), so dropping [standard]
# is a no-op for behavior and resolves the conflict.
uvicorn==0.30.6
pydantic==2.9.2
yfinance>=0.2.40
pandas>=2.2.0
pyotp==2.9.0
git+https://github.com/Kotak-Neo/Kotak-neo-api-v2.git@v2.0.2#egg=neo_api_client
